name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}

main: ${mainClass}

commands:
    lobby:
        description: An example command
        permission: spigotplugin.lobby
        usage: /<command>
    nolobby:
        description: An example command
        permission: spigotplugin.nolobby
        usage: /<command>
    start-hg:
        description: An example command
        permission: spigotplugin.start
        usage: /<command>
    bowman:
        description: An example command
        permission: perm.general
        usage: /<command>
    armored:
        description: An example command
        permission: perm.general
        usage: /<command>
    doglover:
        description: An example command
        permission: perm.general
        usage: /<command>
permissions:
   perm.general:
      default: true
   perm.op:
      default: op